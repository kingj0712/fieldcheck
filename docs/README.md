# docs

Images used by the project `README.md`.

```
docs/
  Screenshots/
    fieldcheck.png     # main workspace (README hero)
    Add_Items.png      # add-item dialog (sections + tags)
    Light_Mode.png     # light theme
```

Reference them from the root README with a relative, case-sensitive path, e.g.:

```markdown
![FieldCheck workspace](docs/Screenshots/fieldcheck.png)
```

Keep files modest in size; the app build is distributed via GitHub Releases, so the repo stays light.
